#include <iostream>
#include <fstream>
#include <vector>
#include <sstream>

using namespace std;

void splitInput(vector<string> const &input, vector<int> &leftSide, vector<int> &rightSide);
